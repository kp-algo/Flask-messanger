apiVersion: v1
kind: Service
metadata:
  name: redis # This makes the hostname "redis" accessible to your web pods
spec:
  selector:
    app: redis
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379